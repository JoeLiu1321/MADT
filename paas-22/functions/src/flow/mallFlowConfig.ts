import * as config from "../config"
export const mallServiceUrl = config.url
export const blockChainUrl = config.blockChainUrl

export const iMallAccount = config.iMallAccount

export const accountBalanceContract = config.accountBalanceContract